import { Box } from '@mui/material';
import React from "react";

const Logo: React.FC = () => {
  return <Box component="img" src="/static/logo.png" sx={{ width: 40, height: 40}} />;
}

export default Logo
